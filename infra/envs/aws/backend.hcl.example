# Remote state for the AWS environment. Uncomment the `backend "s3" {}` block
# in versions.tf, then:
#   terraform init -backend-config=backend.hcl
bucket       = "recoverysearch-tfstate"
key          = "aws/terraform.tfstate"
region       = "us-east-1"
use_lockfile = true
